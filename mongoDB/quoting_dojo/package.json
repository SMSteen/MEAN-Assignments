{
  "name": "quoting_dojo",
  "version": "1.0.0",
  "description": "Week 4 - MongoDB\r Assignment: Quoting Dojo\r Build a simple app that allows users to add quotes to a database and display them on a separate page.\r The 'add my quote' button should add the user's quote to the database, but the 'skip to quotes' button should take the user directly to the main page.  \r Use the following URLs for your project:\r     GET ' / ' for the screen on the left.\r     POST '/quotes' for the method of the form to make a new quote.\r     GET '/quotes' for the screen on the right (where all the quotes are rendered.   \r When you create a new quote, if there are errors in the .save() method, use flash messages to display the errors.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "ejs": "^2.5.9",
    "express": "^4.16.3",
    "express-flash": "0.0.2",
    "express-session": "^1.15.6",
    "moment": "^2.22.1",
    "mongoose": "^5.0.17"
  }
}
