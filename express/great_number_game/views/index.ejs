<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.6/css/bootstrap.min.css" integrity="sha384-rwoIResjU2yc3z8GV/NPeZWAv56rSmLldC3R/AZzGRnGxQQKnKkoFVhFQhNUwEyJ" crossorigin="anonymous">
    <link rel="stylesheet" href="styles/style.css">
    <link rel="shortcut icon" href="images/favicon.ico" type="image/x-icon">
    <title>Great Number Game</title>
</head>
<body>
    <div class="container-fluid">
        <h1>Welcome to the Great Number Game!</h1>
        <h3>I am thinking of a number between 1 and 100.</h3>
        <h3>Take a guess!</h3>
        <% if (guess) { %>
            <% if (guess == random) { %> <!-- user has made a guess -->
                <div class="correct"> <!-- if guessed random number -->
                    <h2 class=>You must be psychic! The number is <%=random %></h2>
                    <form action='/correct' method="POST">
                        <input type="submit" value="Play Again">
                    </form>
                </div>
            <% } else if(guess > random) { %>
                <div class="wrong">
                    <h2>Nope, too high.</h2>
                </div>
            <% } else { %>
                <div class="wrong">
                    <h2>Nope, too low.</h2>
                </div>
            <% } %>
        <% } %>
        <form action='/result' method="POST">
            <input type="number" name="guess">
            <input type="submit" value="Submit">
        </form>
    </div>
    <div class="container-fluid">
        <img class="footer-logo" src="images/favicon.ico" alt="logo"><footer>Polly Designs &trade;</footer>
    </div>
</body>
</html>