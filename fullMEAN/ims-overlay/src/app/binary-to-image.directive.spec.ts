import { BinaryToImageDirective } from './binary-to-image.directive';

describe('BinaryToImageDirective', () => {
  it('should create an instance', () => {
    const directive = new BinaryToImageDirective();
    expect(directive).toBeTruthy();
  });
});
